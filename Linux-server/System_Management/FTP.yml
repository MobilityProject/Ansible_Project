---
- name: Check FTP Anonymous
  ansible.builtin.getent:
    database: passwd
    key: ftp
  register: user_is_exist

- name: Ftp Account is Exists
  fail:
    msg: "Ftp Account is Exists"
  when: user_is_exist is defined
  ignore_errors: yes

    

